export class Song {
    url: string
    title: string
    artist: string

    constructor(url: string, title: string, artist: string) {
        this.url = url
        this.title = title
        this.artist = artist
    }
}